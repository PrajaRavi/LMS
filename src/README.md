# RTL 
1. snapshot testing-->here we basically keep a copy(snapshot) of any file and let say in future if we changed some data in this file then and our test case failed then we can immediatly revert it

2. what we should test=>component rendering,ui element that we write,functions(written by us),api testing,event testing,props and states conditional testing

3. genrally use snapshot testing in the production time

4. RTL(REACT TESTING LIBRARY) query
->i. render component 
  ii. find element in the virtual dom 
  iii. Assertions

5. mocking is nothing but a dummy copy of anything
